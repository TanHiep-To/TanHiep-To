name: Update Readme

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */3 * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
      
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Install dependencies
      run: npm ci

    - name: Run generation script
      run: npm run build

    - name: Commit and push changes
      run: |
        git config --global user.email "tanhiep2012003@gmail.com"
        git config --global user.name "TanHiep-To"
  git add README.md
        git diff --quiet && git diff --staged --quiet || (git commit -m "docs: auto-update profile & quote 🚀" && git push)
